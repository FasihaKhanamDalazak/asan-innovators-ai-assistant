{
    "buildCommand": "npm install && npm run build",
    "outputDirectory": "dist",
    "framework": "vite",
    "env": {
        "VITE_API_BASE_URL": {
            "description": "Base URL for the backend API (e.g., https://your-backend.onrender.com)"
        }
    }
}