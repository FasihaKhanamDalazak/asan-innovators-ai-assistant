services:
  - type: web
    name: asan-ai-assistant-backend
    runtime: python
    pythonVersion: 3.12
    buildCommand: pip install --upgrade pip && pip install -e ./backend
    startCommand: exec cd backend && python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: FRONTEND_URL
        value: https://asan-innovators-ai-assistant.vercel.app
    healthCheckPath: /
